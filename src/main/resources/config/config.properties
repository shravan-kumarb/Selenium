# SauceDemo Selenium + Cucumber configuration.
# Any value can be overridden at runtime, e.g. -Dbrowser=firefox -Dheadless = false

base.url = https://www.saucedemo.com/
api.url = https://jsonplaceholder.typicode.com


#chrome | firefox | edge

browser = chrome
headless=false

#Explicit wait timeout (seconds)
explicit.timeout = 40

#Shared SauceDemo password ( public demo credential; override per environment)
password = secret_sauce

